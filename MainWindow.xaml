<Window x:Class="EDF6ModLoaderWpf.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:EDF6ModLoaderWpf.Helpers"
        mc:Ignorable="d"
        Title="{Binding WindowTitle, FallbackValue='🛡️ EDF Mod Manager'}"
        Height="760" Width="1200"
        MinHeight="620" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        PreviewDragOver="Window_PreviewDragOver"
        PreviewDragLeave="Window_PreviewDragLeave"
        PreviewDrop="Window_PreviewDrop"
        Background="{DynamicResource AppBgBrush}"
        ui:WindowHelper.UseModernWindowStyle="True"
        AutomationProperties.AutomationId="MainWindow"
        AutomationProperties.Name="EDF Mod Manager Main Window">

    <Window.InputBindings>
        <KeyBinding Key="F5"              Command="{Binding DataContext.RefreshCommand,              ElementName=RootWindow}"/>
        <KeyBinding Key="I" Modifiers="Control+Shift" Command="{Binding DataContext.ImportZipCommand,    ElementName=RootWindow}"/>
        <KeyBinding Key="O" Modifiers="Control+Shift" Command="{Binding DataContext.ImportFolderCommand, ElementName=RootWindow}"/>
        <KeyBinding Key="L" Modifiers="Control+Shift" Command="{Binding DataContext.LaunchGameCommand,   ElementName=RootWindow}"/>
        <KeyBinding Key="A" Modifiers="Control+Shift" Command="{Binding DataContext.ApplyModsCommand,    ElementName=RootWindow}"/>
        <KeyBinding Key="A" Modifiers="Control+Alt"   Command="{Binding DataContext.ApplyAndLaunchCommand, ElementName=RootWindow}"/>
    </Window.InputBindings>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>

        <!-- ── Card styles ── -->
        <Style x:Key="PanelCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="CompactCard" TargetType="Border" BasedOn="{StaticResource PanelCard}">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>

        <!-- ── Text styles ── -->
        <Style x:Key="KickerText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        </Style>

        <Style x:Key="SectionTitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        </Style>

        <!-- ── Button styles ── -->
        <Style x:Key="InlineSmallButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,9"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSidebarCollapsed}" Value="True">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="4,9"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeployPrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#6C63FF" BlurRadius="14" ShadowDepth="0" Opacity="0.55"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSidebarCollapsed}" Value="True">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="4,10"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeploySecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,9"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSidebarCollapsed}" Value="True">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="4,9"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- ── DataGrid styles ── -->
        <Style x:Key="DataGridCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModGridRowStyle" TargetType="DataGridRow">
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsActive}" Value="True">
                    <Setter Property="Background" Value="#1022C55E"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsHighRisk}" Value="True">
                    <Setter Property="Background" Value="#0DEF4444"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="ModGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>

        <Style x:Key="CenteredCellText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <!-- ── Recent import card template ── -->
        <DataTemplate x:Key="RecentImportCardTemplate">
            <Border Style="{StaticResource CompactCard}" Margin="0,0,8,0" MinWidth="210">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📦" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1">
                        <DockPanel>
                            <TextBlock Text="{Binding ModName}" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock DockPanel.Dock="Right" Text="📌" Margin="6,0,0,0"
                                       Visibility="{Binding IsPinned, Converter={StaticResource BoolToVis}}"/>
                        </DockPanel>
                        <TextBlock Style="{StaticResource MutedText}" Margin="0,2,0,0">
                            <Run Text="{Binding SourceLabel}"/>
                            <Run Text=" · "/>
                            <Run Text="{Binding ImportedFileCount}"/>
                            <Run Text=" files · "/>
                            <Run Text="{Binding ImportedAtLabel}"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource MutedText}" Margin="0,2,0,0"
                                   Visibility="{Binding ReplacedExisting, Converter={StaticResource BoolToVis}}"
                                   Text="♻️ Replaced existing"/>
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <Button Margin="0,0,4,0"
                                    Command="{Binding DataContext.ToggleRecentImportPinCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" ToolTip="Pin/unpin">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource InlineSmallButton}">
                                        <Setter Property="Content" Value="📌 Pin"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                <Setter Property="Content" Value="📍 Unpin"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="🎯 Select" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                    Tag="RecentImportPrimaryAction"
                                    PreviewKeyDown="RecentImportCardPrimaryAction_PreviewKeyDown"
                                    Command="{Binding DataContext.SelectRecentImportCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"/>
                            <Button Content="📁 Open" Style="{StaticResource InlineSmallButton}"
                                    Command="{Binding DataContext.OpenRecentImportFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <!-- ═══════════════════════════════════════════════════════ -->
    <!--  ROOT: Two-column grid — Sidebar | Main                -->
    <!-- ═══════════════════════════════════════════════════════ -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ══════════════════════════════════ -->
        <!--  SIDEBAR                           -->
        <!-- ══════════════════════════════════ -->
        <Border x:Name="SidebarBorder"
                Grid.Column="0"
                Width="276"
                Background="{DynamicResource SidebarBgBrush}">
            <Grid>
                <!-- Left accent stripe -->
                <Border Width="3" HorizontalAlignment="Left"
                        Background="{DynamicResource AccentBrush}"/>

                <!-- Sidebar content offset past the stripe -->
                <DockPanel Margin="3,0,0,0">

                    <!-- ── Branding (top) ── -->
                    <Border DockPanel.Dock="Top" Padding="10,14,10,12"
                            BorderBrush="{DynamicResource SurfaceBorderBrush}"
                            BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSidebarCollapsed}" Value="True">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="🛡️" FontSize="22" VerticalAlignment="Center"/>
                            <StackPanel Margin="8,0,0,0"
                                        Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}">
                                <TextBlock Text="EDF MOD MGR" FontSize="13" FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="{Binding ActiveGame.ShortName, TargetNullValue=No game selected}" FontSize="10"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ── Scrollable middle sections ── -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="8,12,8,4">

                            <!-- GAME section -->
                            <TextBlock Text="GAME" Style="{StaticResource KickerText}" Margin="4,0,0,6"
                                       Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                            <ItemsControl ItemsSource="{Binding AvailableGames}" Margin="0,0,0,14">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Margin="0,0,0,4"
                                                HorizontalContentAlignment="Left"
                                                Padding="10,9" MinWidth="0" MinHeight="0" FontSize="13"
                                                Command="{Binding DataContext.SwitchGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding GameId}"
                                            AutomationProperties.AutomationId="{Binding GameId, StringFormat=GameSwitchButton_{0}}"
                                            AutomationProperties.Name="{Binding DisplayName, StringFormat=Switch to {0}}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActiveGame}" Value="True">
                                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                            <Setter Property="BorderThickness" Value="2"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DataContext.IsSidebarCollapsed, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                            <Setter Property="Padding" Value="4,9"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" FontSize="16" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="🔴"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding GameId}" Value="EDF41">
                                                                    <Setter Property="Text" Value="🔵"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GameId}" Value="EDF5">
                                                                    <Setter Property="Text" Value="🟢"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <StackPanel Grid.Column="1" Margin="8,0,0,0"
                                                            Visibility="{Binding DataContext.IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}, RelativeSource={RelativeSource AncestorType=Window}}">
                                                    <TextBlock Text="{Binding ShortName}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock FontSize="10">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="⚙️ Setup needed"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding IsConfigured}" Value="True"/>
                                                                            <Condition Binding="{Binding IsActiveGame}" Value="True"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Text" Value="✅ Active"/>
                                                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                                                    </MultiDataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding IsConfigured}" Value="True"/>
                                                                            <Condition Binding="{Binding IsActiveGame}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Text" Value="Ready"/>
                                                                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- STATS section (hidden when collapsed) -->
                            <StackPanel Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}">
                                <TextBlock Text="STATS" Style="{StaticResource KickerText}" Margin="4,0,0,6"/>
                                <UniformGrid Columns="2" Margin="0,0,0,14">
                                    <Border Style="{StaticResource CompactCard}" Margin="0,0,4,4">
                                        <StackPanel>
                                            <TextBlock Text="🧩 Total" Style="{StaticResource KickerText}"/>
                                            <TextBlock Text="{Binding TotalModCount}" FontSize="22" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CompactCard}" Margin="4,0,0,4">
                                        <StackPanel>
                                            <TextBlock Text="✅ Active" Style="{StaticResource KickerText}"/>
                                            <TextBlock Text="{Binding ActiveModCount}" FontSize="22" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource SuccessBrush}"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CompactCard}" Margin="0,0,4,0">
                                        <StackPanel>
                                            <TextBlock Text="⚠️ Conflicts" Style="{StaticResource KickerText}"/>
                                            <TextBlock Text="{Binding ConflictModCount}" FontSize="22" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource WarningBrush}"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CompactCard}" Margin="4,0,0,0">
                                        <StackPanel>
                                            <TextBlock Text="🔥 Risk" Style="{StaticResource KickerText}"/>
                                            <TextBlock Text="{Binding HighRiskModCount}" FontSize="22" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource DangerBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </UniformGrid>
                            </StackPanel>

                            <!-- DEPLOY section -->
                            <TextBlock Text="DEPLOY" Style="{StaticResource KickerText}" Margin="4,0,0,6"
                                       Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>

                            <Button Style="{StaticResource DeployPrimaryButton}" Margin="0,0,0,6"
                                    Command="{Binding ApplyModsCommand}" IsEnabled="{Binding CanApplyMods}"
                                    ToolTip="Apply current mods (Ctrl+Shift+A)"
                                    AutomationProperties.AutomationId="ApplyModsButton"
                                    AutomationProperties.Name="Apply Mods">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🚀" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text=" Apply Mods" VerticalAlignment="Center"
                                               Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DeploySecondaryButton}" Margin="0,0,0,6"
                                    Command="{Binding ApplyAndLaunchCommand}" IsEnabled="{Binding CanApplyAndLaunch}"
                                    ToolTip="Apply mods and launch game (Ctrl+Alt+A)"
                                    AutomationProperties.AutomationId="ApplyAndLaunchButton"
                                    AutomationProperties.Name="Apply And Launch">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🕹️" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text=" Apply + Launch" VerticalAlignment="Center"
                                               Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DeploySecondaryButton}" Margin="0,0,0,10"
                                    Command="{Binding LaunchGameCommand}" IsEnabled="{Binding CanLaunchGame}"
                                    ToolTip="Launch game (Ctrl+Shift+L)"
                                    AutomationProperties.AutomationId="LaunchGameButton"
                                    AutomationProperties.Name="Launch Game">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🎮" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text=" Launch Game" VerticalAlignment="Center"
                                               Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                                </StackPanel>
                            </Button>

                            <!-- Busy indicator -->
                            <DockPanel Margin="4,0,4,0"
                                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                                <ui:ProgressRing DockPanel.Dock="Left" IsActive="{Binding IsBusy}"
                                                 Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding StatusText}" Style="{StaticResource MutedText}"
                                           TextWrapping="Wrap" VerticalAlignment="Center"
                                           Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                            </DockPanel>

                            <Separator Margin="0,14,0,10" Background="{DynamicResource SurfaceBorderBrush}"
                                       Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>

                            <TextBlock Text="APP" Style="{StaticResource KickerText}" Margin="4,0,0,6"
                                       Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>

                            <Button Style="{StaticResource SidebarButtonStyle}"
                                    Command="{Binding OpenSettingsCommand}"
                                    Margin="0,0,0,2" ToolTip="Open settings"
                                    AutomationProperties.AutomationId="OpenSettingsButton"
                                    AutomationProperties.Name="Open Settings">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚙️" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text=" Settings" Margin="4,0,0,0" VerticalAlignment="Center"
                                               Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource SidebarButtonStyle}"
                                    Command="{Binding ToggleThemeCommand}"
                                    Margin="0,0,0,2" ToolTip="Cycle: Dark → Light → System auto"
                                    AutomationProperties.AutomationId="ToggleThemeButton"
                                    AutomationProperties.Name="Toggle Theme">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ThemeButtonIcon}" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ThemeButtonLabel}" Margin="4,0,0,0" VerticalAlignment="Center"
                                               Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource InvBoolToVis}}"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource SidebarButtonStyle}"
                                    Click="ToggleSidebar_Click"
                                    ToolTip="Collapse / expand sidebar"
                                    AutomationProperties.AutomationId="ToggleSidebarButton"
                                    AutomationProperties.Name="Toggle Sidebar">
                                <TextBlock FontSize="14" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="◀ Collapse"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSidebarCollapsed}" Value="True">
                                                    <Setter Property="Text" Value="▶"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>

                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Grid>
        </Border>

        <!-- ══════════════════════════════════ -->
        <!--  MAIN CONTENT                      -->
        <!-- ══════════════════════════════════ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ── Filter + Toolbar Bar ── -->
            <Border Grid.Row="0" Margin="12,12,12,8" Padding="10,8" CornerRadius="10"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource SurfaceBorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Width="240" VerticalContentAlignment="Center"
                                 ToolTip="Search mods, folders, groups, warnings" Margin="0,0,10,0"
                                 AutomationProperties.AutomationId="ModSearchTextBox"
                                 AutomationProperties.Name="Search Mods"/>
                        <CheckBox Content="Active only" IsChecked="{Binding ShowActiveOnly, Mode=TwoWay}"
                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                  AutomationProperties.AutomationId="ShowActiveOnlyCheckBox"
                                  AutomationProperties.Name="Show Active Only"/>
                        <CheckBox Content="Conflicts" IsChecked="{Binding ShowConflictsOnly, Mode=TwoWay}"
                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                  AutomationProperties.AutomationId="ShowConflictsOnlyCheckBox"
                                  AutomationProperties.Name="Show Conflicts Only"/>
                        <Button Content="⚠ Radar" Style="{StaticResource InlineSmallButton}" Margin="0,0,10,0"
                                Command="{Binding ToggleConflictPanelCommand}" ToolTip="Show or hide conflict radar"
                                AutomationProperties.AutomationId="ToggleConflictRadarButton"
                                AutomationProperties.Name="Toggle Conflict Radar"/>
                        <CheckBox Content="High risk" IsChecked="{Binding ShowRiskyOnly, Mode=TwoWay}"
                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                  AutomationProperties.AutomationId="ShowRiskyOnlyCheckBox"
                                  AutomationProperties.Name="Show Risky Only"/>
                        <Button Content="Clear" Style="{StaticResource InlineSmallButton}" Margin="0,0,10,0"
                                Command="{Binding ClearFiltersCommand}" ToolTip="Clear all filters"
                                AutomationProperties.AutomationId="ClearFiltersButton"
                                AutomationProperties.Name="Clear Filters"/>
                    </WrapPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Button Content="🔄" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                Command="{Binding RefreshCommand}" ToolTip="Refresh (F5)"
                                AutomationProperties.AutomationId="RefreshButton"
                                AutomationProperties.Name="Refresh"/>
                        <Button Content="📥 Import ▾" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                Click="ToolbarMenuButton_Click"
                                AutomationProperties.AutomationId="ImportMenuButton"
                                AutomationProperties.Name="Import Menu">
                            <Button.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="📦 Zip Archive..." Command="{Binding ImportZipCommand}" InputGestureText="Ctrl+Shift+I"/>
                                    <MenuItem Header="📁 Folder..."      Command="{Binding ImportFolderCommand}" InputGestureText="Ctrl+Shift+O"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <Button Content="🧰 Manage ▾" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                Click="ToolbarMenuButton_Click"
                                AutomationProperties.AutomationId="ManageMenuButton"
                                AutomationProperties.Name="Manage Menu">
                            <Button.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="✅ Enable All"       Command="{Binding EnableAllCommand}"/>
                                    <MenuItem Header="⛔ Disable All"      Command="{Binding DisableAllCommand}"/>
                                    <Separator/>
                                    <MenuItem Header="🔤 Sort by Name"     Command="{Binding SortByNameCommand}"/>
                                    <MenuItem Header="↩️ Undo Last Change" Command="{Binding UndoCommand}" IsEnabled="{Binding CanUndo}"/>
                                    <Separator/>
                                    <MenuItem Header="🛟 Restore Last Apply" Command="{Binding RestoreLastApplyBackupCommand}" IsEnabled="{Binding CanRestoreLastApply}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <Button Content="🗂️ View ▾" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                Click="ToolbarMenuButton_Click"
                                AutomationProperties.AutomationId="ViewMenuButton"
                                AutomationProperties.Name="View Menu">
                            <Button.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="⬆️ Load Order View" Command="{Binding ShowLoadOrderViewCommand}"/>
                                    <MenuItem Header="📋 All Mods View"   Command="{Binding ShowAllModsViewCommand}"/>
                                    <MenuItem Header="📂 Group View"      Command="{Binding ToggleGroupViewCommand}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <Button x:Name="LoadoutButton" Content="📚 Loadout ▾"
                                Style="{StaticResource InlineSmallButton}" Click="LoadoutButton_Click"
                                AutomationProperties.AutomationId="LoadoutButton"
                                AutomationProperties.Name="Loadout Menu"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Loadout Popup -->
                        <Popup x:Name="LoadoutPopup" PlacementTarget="{Binding ElementName=LoadoutButton}"
                                     Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                                     AutomationProperties.AutomationId="LoadoutPopup">
                <Border Margin="4" Padding="14" CornerRadius="10"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel Width="300">
                        <DockPanel Margin="0,0,0,10">
                            <TextBlock Text="📚 Loadout" Style="{StaticResource SectionTitleText}"/>
                            <TextBlock DockPanel.Dock="Right" Style="{StaticResource MutedText}" VerticalAlignment="Center">
                                <Run Text="Active: "/>
                                <Run Text="{Binding ActivePresetDisplayName, Mode=OneWay}"/>
                            </TextBlock>
                        </DockPanel>
                        <TextBlock Text="✏️ Modified" Style="{StaticResource MutedText}"
                                   Foreground="{DynamicResource WarningBrush}" Margin="0,0,0,8"
                                   Visibility="{Binding IsActivePresetDirty, Converter={StaticResource BoolToVis}}"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding AvailablePresets}"
                                      SelectedItem="{Binding SelectedPresetName, Mode=TwoWay}" VerticalAlignment="Center"
                                      AutomationProperties.AutomationId="PresetSelectorComboBox"
                                      AutomationProperties.Name="Preset Selector"/>
                            <TextBox Grid.Column="2" Text="{Binding PresetNameInput, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalContentAlignment="Center" ToolTip="New preset name"
                                     AutomationProperties.AutomationId="PresetNameTextBox"
                                     AutomationProperties.Name="Preset Name"/>
                        </Grid>
                        <WrapPanel>
                                <Button Content="💾 Save"   Command="{Binding SavePresetCommand}"   IsEnabled="{Binding CanSavePreset}"   Style="{StaticResource InlineSmallButton}" Margin="0,0,6,0"
                                    AutomationProperties.AutomationId="SavePresetButton"
                                    AutomationProperties.Name="Save Preset"/>
                                <Button Content="📥 Load"   Command="{Binding LoadPresetCommand}"   IsEnabled="{Binding CanLoadPreset}"   Style="{StaticResource InlineSmallButton}" Margin="0,0,6,0"
                                    AutomationProperties.AutomationId="LoadPresetButton"
                                    AutomationProperties.Name="Load Preset"/>
                                <Button Content="✏️ Rename" Command="{Binding RenamePresetCommand}" IsEnabled="{Binding CanRenamePreset}" Style="{StaticResource InlineSmallButton}" Margin="0,0,6,0"
                                    AutomationProperties.AutomationId="RenamePresetButton"
                                    AutomationProperties.Name="Rename Preset"/>
                                <Button Content="🗑️ Delete" Command="{Binding DeletePresetCommand}" IsEnabled="{Binding CanDeletePreset}" Style="{StaticResource InlineSmallButton}"
                                    AutomationProperties.AutomationId="DeletePresetButton"
                                    AutomationProperties.Name="Delete Preset"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </Popup>

            <!-- ── Batch Import Progress ── -->
            <Border Grid.Row="1" Margin="12,0,12,8" Padding="14" CornerRadius="10"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource SurfaceBorderBrush}" BorderThickness="1"
                    Visibility="{Binding IsBatchImportActive, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:ProgressRing Grid.Column="0" IsActive="True" Width="24" Height="24"
                                     Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding BatchImportProgressLabel}" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding BatchImportCurrentItemLabel}"
                                   Style="{StaticResource MutedText}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ── Mod DataGrid ── -->
            <DataGrid x:Name="ModsDataGrid"
                      Grid.Row="2"
                      ItemsSource="{Binding Mods}"
                      SelectedItem="{Binding SelectedMod, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Margin="12,0,12,0"
                      VerticalScrollBarVisibility="Auto"
                      AllowDrop="True"
                      RowStyle="{StaticResource ModGridRowStyle}"
                      CellStyle="{StaticResource ModGridCellStyle}"
                      ColumnHeaderStyle="{StaticResource ModGridHeaderStyle}"
                      Background="{DynamicResource AppBgBrush}"
                      AutomationProperties.AutomationId="ModsDataGrid"
                      AutomationProperties.Name="Mods Grid"
                      PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                      PreviewMouseMove="DataGrid_PreviewMouseMove"
                      DragOver="DataGrid_DragOver"
                      Drop="DataGrid_Drop">

                <DataGrid.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.ContainerStyle>
                            <Style TargetType="GroupItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GroupItem">
                                            <Expander IsExpanded="True" Margin="0,0,0,4"
                                                      BorderThickness="0,0,0,1"
                                                      BorderBrush="{DynamicResource SurfaceBorderBrush}">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📂 " FontSize="14"/>
                                                        <TextBlock FontWeight="SemiBold" FontSize="13"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                                   VerticalAlignment="Center">
                                                            <TextBlock.Text>
                                                                <MultiBinding StringFormat="{}{0} ({1} mods)">
                                                                    <Binding Path="Name"/>
                                                                    <Binding Path="ItemCount"/>
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <ItemsPresenter/>
                                            </Expander>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.ContainerStyle>
                    </GroupStyle>
                </DataGrid.GroupStyle>

                <DataGrid.Columns>
                    <!-- Active checkbox -->
                    <DataGridTemplateColumn Header="On" Width="52" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource DataGridCheckBoxStyle}"
                                          Click="ModCheckBox_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Load order badge -->
                    <DataGridTemplateColumn Header="Order" Width="68" SortMemberPath="LoadOrder">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="6,3" Background="{DynamicResource SurfaceBrush}">
                                    <TextBlock Text="{Binding LoadOrderDisplay}" FontWeight="SemiBold"
                                               Foreground="{DynamicResource AccentBrush}"
                                               Style="{StaticResource CenteredCellText}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding LoadOrder, UpdateSourceTrigger=LostFocus}"
                                         HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                         HorizontalContentAlignment="Center"
                                         LostFocus="LoadOrderTextBox_LostFocus"
                                         PreviewTextInput="LoadOrderTextBox_PreviewTextInput"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Mod name + description -->
                    <DataGridTemplateColumn Header="Mod" Width="*" MinWidth="220" SortMemberPath="ModName" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding ModName}" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource MutedText}"
                                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Group (cyan, editable) -->
                    <DataGridTemplateColumn Header="Group" Width="Auto" MinWidth="104" SortMemberPath="Group">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Group}" FontStyle="Italic"
                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                           Foreground="{DynamicResource AccentCyanBrush}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Group, UpdateSourceTrigger=Explicit}"
                                         HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                         LostFocus="GroupTextBox_LostFocus"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Subfolders -->
                    <DataGridTextColumn Header="Folders" Binding="{Binding Subfolders}"
                                        Width="Auto" MinWidth="132" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Warning indicator -->
                    <DataGridTemplateColumn Header="Warnings" Width="84" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Cursor="Hand" FontSize="16">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value=""/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasWarnings}" Value="True">
                                                    <Setter Property="Text" Value="⚠️"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CompatibilityStatus}" Value="Mismatch">
                                                    <Setter Property="Text" Value="⛔"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsHighRisk}" Value="True">
                                                    <Setter Property="Text" Value="🔥"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <TextBlock.ToolTip>
                                        <ToolTip MaxWidth="340">
                                            <TextBlock Text="{Binding WarningDetails}" TextWrapping="Wrap"/>
                                        </ToolTip>
                                    </TextBlock.ToolTip>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Status pill -->
                    <DataGridTemplateColumn Header="Status" Width="Auto" MinWidth="76" SortMemberPath="Status">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="8,4"
                                        HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Active">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="✅ Wins">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="⚠️ Overridden">
                                                    <Setter Property="Background" Value="{DynamicResource WarningBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource CenteredCellText}">
                                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Active">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="✅ Wins">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="⚠️ Overridden">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Move up/down -->
                    <DataGridTemplateColumn Header="Move" Width="92" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="⬆️" Style="{StaticResource InlineSmallButton}" Margin="0,0,4,0"
                                            ToolTip="Move up"
                                            AutomationProperties.AutomationId="MoveUpButton"
                                            AutomationProperties.Name="Move Up"
                                            Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="⬇️" Style="{StaticResource InlineSmallButton}"
                                            ToolTip="Move down"
                                            AutomationProperties.AutomationId="MoveDownButton"
                                            AutomationProperties.Name="Move Down"
                                            Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Info tooltip -->
                    <DataGridTemplateColumn Header="Info" Width="54" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="ℹ️" HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Cursor="Hand" FontSize="15">
                                    <TextBlock.ToolTip>
                                        <ToolTip MaxWidth="360">
                                            <StackPanel>
                                                <TextBlock Text="{Binding ModName}" FontWeight="Bold" Margin="0,0,0,4"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                <TextBlock Margin="0,8,0,0" FontSize="11" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource TextPrimaryBrush}">
                                                    <Run Text="Compatibility: "/><Run Text="{Binding CompatibilityStatus, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}">
                                                    <Run Text="Risk: "/><Run Text="{Binding RiskLevel, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock Margin="0,4,0,0" FontSize="11" TextWrapping="Wrap"
                                                           Foreground="{DynamicResource TextMutedBrush}">
                                                    <Run Text="Warnings: "/><Run Text="{Binding WarningDetails, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock Margin="0,4,0,0" FontSize="11"
                                                           Foreground="{DynamicResource TextMutedBrush}">
                                                    <Run Text="Subfolders: "/><Run Text="{Binding Subfolders, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="11" Foreground="{DynamicResource TextMutedBrush}">
                                                    <Run Text="Date: "/><Run Text="{Binding DateAdded, StringFormat=yyyy-MM-dd, Mode=OneWay}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </TextBlock.ToolTip>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

                <Border Grid.Row="2" Margin="12,0,12,0" Panel.ZIndex="1"
                    AutomationProperties.AutomationId="EmptyLibraryState">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource PanelCard}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding TotalModCount}" Value="0"/>
                                    <Condition Binding="{Binding IsBusy}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid MinHeight="320">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
                        <TextBlock Text="🧩" FontSize="44" HorizontalAlignment="Center"/>
                        <TextBlock Text="Your mod library is empty"
                                   FontSize="24" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Margin="0,12,0,0" TextAlignment="Center"/>
                        <TextBlock Margin="0,10,0,0"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   TextWrapping="Wrap" TextAlignment="Center"
                                   Text="Import a zip archive or an existing folder to start tracking load order, conflicts, and deploy status in one place."/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                            <Button Content="📦 Import Zip" Style="{StaticResource InlineSmallButton}" Margin="0,0,8,0"
                                    Command="{Binding ImportZipCommand}"/>
                            <Button Content="📁 Import Folder" Style="{StaticResource InlineSmallButton}"
                                    Command="{Binding ImportFolderCommand}"/>
                        </StackPanel>
                        <TextBlock Margin="0,12,0,0" Style="{StaticResource MutedText}" TextAlignment="Center"
                                   Text="Shortcuts: Ctrl+Shift+I imports a zip, Ctrl+Shift+O imports a folder."/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ── Recent Imports Drawer ── -->
            <Border Grid.Row="3" Margin="12,8,12,0" CornerRadius="10"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource SurfaceBorderBrush}" BorderThickness="1"
                    AutomationProperties.AutomationId="RecentImportsDrawer"
                    Visibility="{Binding HasRecentImports, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <Border x:Name="RecentImportsHeader" Padding="12,10" Cursor="Hand"
                            MouseLeftButtonUp="RecentImportsHeader_Click">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="🧹 Clear" Style="{StaticResource InlineSmallButton}" Margin="0,0,8,0"
                                    Command="{Binding ClearRecentImportsCommand}" IsEnabled="{Binding HasRecentImports}"
                                    AutomationProperties.AutomationId="ClearRecentImportsButton"
                                    AutomationProperties.Name="Clear Recent Imports"/>
                                <TextBlock x:Name="RecentImportsChevron" Text="▲" FontSize="11"
                                           Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="🕘 Recent Imports" Style="{StaticResource SectionTitleText}"/>
                        </DockPanel>
                    </Border>
                    <Border x:Name="RecentImportsContent" Padding="12,0,12,12">
                        <StackPanel>
                            <!-- Latest highlight -->
                            <Border Style="{StaticResource CompactCard}" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="✨" FontSize="22" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}">
                                            <Run Text="Latest: "/><Run Text="{Binding LatestRecentImport.ModName}"/>
                                        </TextBlock>
                                        <TextBlock Style="{StaticResource MutedText}" Margin="0,2,0,0">
                                            <Run Text="{Binding LatestRecentImport.SourceLabel}"/>
                                            <Run Text=" · "/>
                                            <Run Text="{Binding LatestRecentImport.ImportedAtLabel}"/>
                                            <Run Text=" · "/>
                                            <Run Text="{Binding LatestRecentImport.ImportedFileCount}"/>
                                            <Run Text=" files"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <Button Margin="0,0,6,0"
                                                Command="{Binding ToggleRecentImportPinCommand}"
                                            CommandParameter="{Binding LatestRecentImport}"
                                            AutomationProperties.AutomationId="ToggleLatestRecentImportPinButton"
                                            AutomationProperties.Name="Toggle Latest Recent Import Pin">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource InlineSmallButton}">
                                                    <Setter Property="Content" Value="📌 Pin"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LatestRecentImport.IsPinned}" Value="True">
                                                            <Setter Property="Content" Value="📍 Unpin"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="🎯 Jump" Margin="0,0,6,0" Style="{StaticResource InlineSmallButton}"
                                                Tag="RecentImportPrimaryAction"
                                                PreviewKeyDown="RecentImportCardPrimaryAction_PreviewKeyDown"
                                                Command="{Binding SelectRecentImportCommand}"
                                            CommandParameter="{Binding LatestRecentImport}"
                                            AutomationProperties.AutomationId="SelectLatestRecentImportButton"
                                            AutomationProperties.Name="Select Latest Recent Import"/>
                                        <Button Content="📁 Open" Style="{StaticResource InlineSmallButton}"
                                                Command="{Binding OpenRecentImportFolderCommand}"
                                            CommandParameter="{Binding LatestRecentImport}"
                                            AutomationProperties.AutomationId="OpenLatestRecentImportFolderButton"
                                            AutomationProperties.Name="Open Latest Recent Import Folder"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <!-- Pinned -->
                            <StackPanel Visibility="{Binding HasPinnedRecentImports, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding PinnedRecentImportsHeader}"
                                           Style="{StaticResource KickerText}" Margin="0,0,0,6"/>
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding PinnedRecentImports}"
                                                  ItemTemplate="{StaticResource RecentImportCardTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                            <!-- Unpinned -->
                            <StackPanel Visibility="{Binding HasUnpinnedRecentImports, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding RecentImportsHeader}"
                                           Style="{StaticResource KickerText}" Margin="0,4,0,6"/>
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding RecentImports}"
                                                  ItemTemplate="{StaticResource RecentImportCardTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ── Conflict Radar Drawer ── -->
            <Border Grid.Row="4" Margin="12,8,12,0" CornerRadius="10"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1"
                    AutomationProperties.AutomationId="ConflictRadarDrawer"
                    Visibility="{Binding IsConflictPanelVisible, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <Border Padding="12,10" Cursor="Hand" MouseLeftButtonUp="ConflictRadarHeader_Click">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="✕ Hide" Style="{StaticResource InlineSmallButton}" Margin="0,0,8,0"
                                    Command="{Binding HideConflictPanelCommand}"
                                    AutomationProperties.AutomationId="HideConflictRadarButton"
                                    AutomationProperties.Name="Hide Conflict Radar"/>
                                <TextBlock x:Name="ConflictRadarChevron" Text="▲" FontSize="11"
                                           Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="⚠️ Conflict Radar" Style="{StaticResource SectionTitleText}"/>
                        </DockPanel>
                    </Border>
                    <Border x:Name="ConflictRadarContent" Padding="12,0,12,12">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding ConflictReport}">
                                <ItemsControl.Template>
                                    <ControlTemplate TargetType="ItemsControl">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="190">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </ItemsControl.Template>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CompactCard}" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap"
                                                               Foreground="{DynamicResource TextPrimaryBrush}">
                                                        <Run Text="📄 "/><Run Text="{Binding RelativePath, Mode=OneWay}"/>
                                                    </TextBlock>
                                                    <TextBlock Margin="0,4,0,0" FontSize="11" TextWrapping="Wrap"
                                                               Foreground="{DynamicResource TextPrimaryBrush}">
                                                        <Run Text="🏆 Winner: "/>
                                                        <Run Text="{Binding WinnerModName, Mode=OneWay}" FontWeight="SemiBold"/>
                                                        <Run Text=" (#"/><Run Text="{Binding WinnerLoadOrder, Mode=OneWay}"/><Run Text=")"/>
                                                    </TextBlock>
                                                    <TextBlock FontSize="11" TextWrapping="Wrap"
                                                               Foreground="{DynamicResource TextMutedBrush}">
                                                        <Run Text="🧯 Loser: "/>
                                                        <Run Text="{Binding LoserModName, Mode=OneWay}" FontWeight="SemiBold"/>
                                                        <Run Text=" (#"/><Run Text="{Binding LoserLoadOrder, Mode=OneWay}"/><Run Text=")"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                                                            Orientation="Horizontal">
                                                    <Button Width="30" Height="30" Padding="4" Margin="0,0,4,0" FontSize="14"
                                                            Style="{StaticResource InlineSmallButton}"
                                                            ToolTip="Select Winner"
                                                            AutomationProperties.AutomationId="SelectWinnerButton"
                                                            AutomationProperties.Name="Select Winner"
                                                            Command="{Binding DataContext.SelectConflictWinnerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">🏆</Button>
                                                    <Button Width="30" Height="30" Padding="4" Margin="0,0,4,0" FontSize="14"
                                                            Style="{StaticResource InlineSmallButton}"
                                                            ToolTip="Select Loser"
                                                            AutomationProperties.AutomationId="SelectLoserButton"
                                                            AutomationProperties.Name="Select Loser"
                                                            Command="{Binding DataContext.SelectConflictLoserCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">🧯</Button>
                                                    <Button Width="30" Height="30" Padding="4" Margin="0,0,4,0" FontSize="14"
                                                            Style="{StaticResource InlineSmallButton}"
                                                            ToolTip="Make Loser Win"
                                                            AutomationProperties.AutomationId="MakeLoserWinButton"
                                                            AutomationProperties.Name="Make Loser Win"
                                                            Command="{Binding DataContext.MakeLoserWinCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">⬆</Button>
                                                    <Button Width="30" Height="30" Padding="4" FontSize="14"
                                                            Style="{StaticResource InlineSmallButton}"
                                                            ToolTip="Lower Winner Priority"
                                                            AutomationProperties.AutomationId="MoveWinnerDownButton"
                                                            AutomationProperties.Name="Move Winner Down"
                                                            Command="{Binding DataContext.MoveWinnerDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">⬇</Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Margin="0,6,0,0" Style="{StaticResource MutedText}"
                                       Text="💡 Higher load order wins file conflicts. Use the action buttons, then apply when ready."/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ── Toast Panel ── -->
            <StackPanel x:Name="ToastPanel" Grid.Row="5" Margin="12,8,12,0"/>

            <!-- ── Status Bar ── -->
            <Border Grid.Row="6" Margin="0,8,0,0" Padding="14,8"
                    Background="{DynamicResource StatusBarBgBrush}"
                    BorderBrush="{DynamicResource SurfaceBorderBrush}" BorderThickness="0,1,0,0">
                <DockPanel>
                    <ui:ProgressRing DockPanel.Dock="Right" IsActive="{Binding IsBusy}"
                                     Width="18" Height="18" Margin="8,0,0,0" VerticalAlignment="Center"
                                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Text="🛰️" Margin="0,0,8,0" VerticalAlignment="Center"
                               Foreground="{DynamicResource AccentBrush}"/>
                    <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>
        </Grid>

        <!-- ══════════════════════════════════ -->
        <!--  DROP OVERLAY (spans both cols)    -->
        <!-- ══════════════════════════════════ -->
        <Grid x:Name="DropOverlay" Grid.ColumnSpan="2" Panel.ZIndex="100"
              Visibility="Collapsed" IsHitTestVisible="False"
              AutomationProperties.AutomationId="DropOverlay">
            <Border Margin="24" CornerRadius="18" BorderThickness="3"
                    BorderBrush="{DynamicResource AccentBrush}"
                    Background="{DynamicResource SurfaceBrush}" Opacity="0.97">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="460">
                    <TextBlock Text="📥" FontSize="46" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock x:Name="DropOverlayTitle" Text="Release to import"
                               FontSize="26" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}" TextAlignment="Center"/>
                    <TextBlock x:Name="DropOverlayText" Margin="0,10,0,0" FontSize="13"
                               Foreground="{DynamicResource TextMutedBrush}"
                               TextAlignment="Center" TextWrapping="Wrap"
                               Text="Your import will be previewed before it joins the library."/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
